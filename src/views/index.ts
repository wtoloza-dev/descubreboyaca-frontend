/**
 * Views Index
 *
 * Export all views.
 *
 * Views vs Pages:
 * - Views contain business logic, state management, and components
 * - Pages (in app/) are just routing wrappers
 * - This allows views to be reusable and testable
 */

export { LoginView } from './Login';
export { ProfileView } from './Profile';
export { RestaurantDetailView } from './RestaurantDetail';
export { RestaurantsView } from './Restaurants';
