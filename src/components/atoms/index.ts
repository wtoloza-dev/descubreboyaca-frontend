/**
 * Atoms Index
 * 
 * Exports all atomic components (atoms).
 * Atoms are the most basic and indivisible components.
 */

export { Button } from './button';
export type { ButtonProps } from './button';

